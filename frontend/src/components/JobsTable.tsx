import React from 'react';
import { Link } from "react-router-dom";
import { Description, Title } from "./layout";

export function JobsTable() {
  /*return (
    <SubPage>
      <Title>Lista zamówień</Title>
      <Description>Na tej zakładce sprawdzisz stan złożonych zamówień</Description>
      <form onSubmit={handleSubmit(onSubmit)} className="mx-auto grid grid-cols-3 gap-8">
        <div className="my-auto">
          Data od: <input type="date" className="border-2" {...register('startDate')} />
        </div>
        <div className="my-auto">
          Data do: <input type="date" className="border-2" {...register('endDate')} />
        </div>
        <div>
          <button className="btn-primary btn-sm  btn">załaduj</button>
        </div>
      </form>
      <OrdersTable {...currentDates} />
      <div className="card-actions justify-end p-4">
        <Link to="/" className="btn-secondary btn w-32">
          Powrót
        </Link>
      </div>
    </SubPage>
  )*/
  return null;
}