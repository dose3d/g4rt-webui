# Devcontainer configuration

Upon entering devcontainer environment (frontend, backend, db) is build automatically.

## Requirements

Dependencies for npm are needed to be installed first.

1. `cd` into jsroot-fork
    1. `npm install`
    2. `nmp build`
2. `cd` into frontend
    1. `npm install`
    2. `npm build`
